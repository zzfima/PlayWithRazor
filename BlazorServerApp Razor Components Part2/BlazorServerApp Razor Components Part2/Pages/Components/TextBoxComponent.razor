@* component use arbitrary parameters *@

<input @attributes="@CustomInputAttributes" />

@code {
	[Parameter(CaptureUnmatchedValues = true)]
	public Dictionary<string, object>? CustomInputAttributes { get; set; }
}