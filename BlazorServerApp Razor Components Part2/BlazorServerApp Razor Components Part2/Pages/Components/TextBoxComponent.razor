@* component use arbitrary parameters *@

<input @attributes="@CustomArbitraryAttributes" />

@code {
	[Parameter(CaptureUnmatchedValues = true)]
	public Dictionary<string, object>? CustomArbitraryAttributes { get; set; }
}